# Doctors Manager for WordPress

Тестовое задание на разработку каталога врачей с фильтрацией и кастомными типами записей.

## 📋 Описание решения

Реализован плагин и тема для управления каталогом специалистов.

* **CPT:** `doctors`
* **Таксономии:** Специализация (hierarchical), Город (hierarchical)
* **Поля (ACF):** Стаж, Рейтинг, Цена
* **Оптимизация:** Фильтрация реализована через `pre_get_posts` (избегая лишних вторичных запросов `WP_Query`).

---

## 🚀 Установка и запуск

Вы можете развернуть проект двумя способами: через Docker Compose (рекомендуется) или вручную.

### Способ 1: Быстрый запуск через Docker 🐳

Для быстрого старта в корне проекта предусмотрен файл конфигурации `docker-compose.yml` (WordPress + MariaDB).

1.  **Запуск контейнеров:**
    Откройте терминал в папке проекта и выполните:
    ```bash
    docker-compose up -d
    ```

2.  **Установка WordPress:**
    Перейдите по адресу [http://localhost:8080](http://localhost:8080).
    * Пройдите стандартную установку.
    * БД подключится автоматически.

3.  **Активация:**
    * В админке перейдите в **Внешний вид → Темы** и активируйте `doctors-theme`.
    * Перейдите в **Плагины** и активируйте `doctors-manager`.
    * Установите и активируйте плагин **Advanced Custom Fields (ACF)**.

4.  **Остановка:**
    ```bash
    docker-compose down
    ```

### Способ 2: Ручная установка 🛠

1.  Скопируйте папку `doctors-manager` в `/wp-content/plugins/`.
2.  Скопируйте папку `doctors-theme` в `/wp-content/themes/`.
3.  Активируйте тему и плагин в админ-панели WordPress.
4.  Установите плагин **ACF**.

---

## ⚙️ Настройка и Импорт данных

Для корректной работы каталога необходимо выполнить следующие действия после установки:

### 1. Импорт полей (ACF)
1.  Перейдите в **ACF → Инструменты → Импорт**.
2.  Выберите файл `acf-export.json` (находится в корне репозитория).
3.  Нажмите **«Импорт JSON»**.

### 2. Импорт демо-данных
В репозитории находится файл с тестовыми врачами `doctors-demo-data.xml`.
1.  Перейдите в **Инструменты → Импорт**.
2.  Выберите **WordPress** (установите импортер, если нужно).
3.  Загрузите файл `doctors-demo-data`, запустите импорт, поставьте галочку «Скачать и импортировать файлы вложений».

### 3. Настройка ссылок
1.  Перейдите в **Настройки → Постоянные ссылки**.
2.  Просто нажмите кнопку **«Сохранить изменения»**.
    *Это обязательно для обновления правил перезаписи (rewrite rules), чтобы страницы врачей `/doctors/` открывались корректно.*

---

## 🛠 Технические детали

* **Фильтрация:** Работает через нативные GET-параметры URL.
* **Безопасность:** Реализована защита данных через `sanitize_text_field` и `esc_html`.
* **Вёрстка:** Адаптивная (CSS Grid/Flexbox).

## 🔗 Проверка

После настройки страница архива врачей будет доступна по адресу:
`/doctors/` (или `http://localhost:8080/doctors/` при использовании Docker).
