# Doctors Manager for WordPress

Тестовое задание на разработку каталога врачей с фильтрацией и кастомными типами записей.

## 📋 Описание решения
Реализован плагин и тема для управления каталогом специалистов.
- **CPT:** `doctors`
- **Таксономии:** Специализация (hierarchical), Город (hierarchical)
- **Поля (ACF):** Стаж, Рейтинг, Цена
- **Оптимизация:** Фильтрация через `pre_get_posts` (без лишних `WP_Query`)

## 🚀 Установка

1. Скопируйте папку `doctors-manager` в `/wp-content/plugins/`.
2. Скопируйте папку `doctors-theme` в `/wp-content/themes/`.
3. Активируйте тему и плагин в админке.
4. Импортируйте поля: **ACF → Инструменты → Импорт** (файл `acf-export.json`).
5. Сохраните пермалинки: **Настройки → Постоянные ссылки → Сохранить**.

## 🛠 Технические детали
- Фильтрация работает через нативные GET-параметры.
- Реализована защита данных через `sanitize_text_field` и `esc_html`.
- Вёрстка адаптивная (CSS Grid/Flexbox).

## 🔗 Проверка
Страница архива: `/doctors/`
